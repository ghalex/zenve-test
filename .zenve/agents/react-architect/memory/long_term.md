# React Architect Long-Term Memory

- The SPA lives under `ui/`, not the workspace root; React planning artifacts should still be written under `docs/plans/react/` and `docs/tasks/react/`.
- The public booking flow currently lives on `ui/src/pages/public-booking-page.tsx` and uses the `booking` Redux domain plus RTK Query in `ui/src/store/booking/`.
- The project visual rules for frontend planning live at `.zenve/agents/react-dev/DESIGN.md`.
