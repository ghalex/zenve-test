# Scratch

- Planned `docs/product/guest-timezone-selection.md` against the existing `ui/` public booking flow.
- Created one feature plan and three ordered `react-dev` tasks for timezone contract, selector/context UI, and end-to-end display-timezone consistency.
