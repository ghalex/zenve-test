# Scratch

- Issue #36 implemented in `ui/`: settings route, settings store API, avatar upload helper, and settings UI components.
- Issue #39 implemented in `ui/`: private routes use `components/dashboard/DashboardLayout`; sidebar primitives were added under `src/components/ui`; public booking/auth routes stay top-level.
- Issue #42 implemented in `ui/`: added `src/pages/hello.tsx` and protected `/hello` route.
