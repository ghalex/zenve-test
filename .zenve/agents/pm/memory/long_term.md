# PM Long-Term Memory

- The product is Zenve, a scheduling tool similar to Calendly where hosts share availability links and guests book time slots.
- Existing product PRDs use the personas `Host` and `Guest`.
- Issue `#36` is scoped as a first-release host settings page focused on profile details: name and avatar.
- The settings page PRD defines host name validation as required trimmed text with a 1 to 80 character limit.
