# Design Reference

This frontend follows the design system documented in `.zenve/agents/frontend-dev/DESIGN.md`.

This file exists so project-level React architecture checks can discover a root `DESIGN.md` within the `ui/` app.

Use `.zenve/agents/frontend-dev/DESIGN.md` as the authoritative source for colors, typography, spacing, and component patterns.
